const numbers = [1, 2, 3, 4];
const numbersDouble = numbers.map(number => number * 2)

class Persona {
    constructor (nombre) {
        this.nombre = nombre;
    }
};

console.log(numbers);

console.log('linea de prueba');

console.log('cuarta linea de prueba');